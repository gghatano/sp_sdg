# Copy to .env and adjust as needed. No secrets are required for this project.

# Where UCR datasets are cached (default: data/raw)
DATA_DIR=data/raw

# Torch thread count for CPU training (empty = library default)
OMP_NUM_THREADS=
